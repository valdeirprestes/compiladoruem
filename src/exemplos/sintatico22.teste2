int main() {
    int i = 1;
    if( i )
        if(i)
            return i;
    else{
        main();
    }


    if(i)
        i=1;
    else if( i )
        i = 1;
    else
        i= 1;
    while(i){
        break;
    }
    int num1;
    switch (  num1 ){
        case 1:
            acao(1);
            break;
        case 2:
            acao(2);
            break;
        default:
            acao(0);
            break;
    }
}
